*   Use Node http module to handle socket connections and parse HTTP requests.
*   Respond to GET requests with a static 'page'.
*   To test:
    *   Run `node server.js`.
    *   Point browser at `http://localhost:8080/`
